apiVersion: apps/v1
kind: Deployment
metadata:
  name: deploy-portfolio
  namespace: portfolio-namespace
spec:
  replicas: 1
  selector:
    matchLabels:
      app: miportfolio
  template:
    metadata:
      labels:
        app: miportfolio
    spec:
      volumes:
        - name: logs
          hostPath:
            path: /var/log/miportfolio 
            type: DirectoryOrCreate
      containers:
        - name: miportfolio
          image: iferlop/portfolioreact:a3e1c8c77e01d5402e80588cab3d881334e7273c
          imagePullPolicy: Always
          ports:
            - containerPort: 80
          volumeMounts:
            - name: logs
              mountPath: /var/log/nginx
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"