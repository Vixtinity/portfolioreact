services:
  portfolio:
    build: 
      context: ../..
      dockerfile: deploy/build_img/Dockerfile
    image: ismaelfernandez/portfolioreact:v1
    container_name: portfolio_app
    ports: 
      - "8000:8000"
    restart: always